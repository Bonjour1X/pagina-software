<br>
<h1 class="title has-text-centered custom-title" style="color: #010c55; font-size: 48px;">
  <%= image_tag 'sports.png', alt: 'Imagen izquierda', class: 'title-img-left' %>
  Solicitudes de inscripción
  <%= image_tag 'sports.png', alt: 'Imagen derecha', class: 'title-img-right' %>
</h1>
<br>



<% @enrollment_requests.each do |request| %>
  <div style="display: flex; align-items: center; gap: 10px;">
    <h1 style="margin: 0;">Usuario: <%= request.user.name %></h1>
    <%= button_to 'Aprobar', approve_course_enrollment_request_path(@course, request), method: :patch, style: 'background-color: #2196F3; color: white; font-size: 0.875rem; padding: 4px 10px; border: none; border-radius: 4px;' %>
    <%= button_to 'Rechazar', reject_course_enrollment_request_path(@course, request), method: :patch, style: 'background-color: #f44336; color: white; font-size: 0.875rem; padding: 4px 10px; border: none; border-radius: 4px;' %>
  </div>
<% end %>

<br>
<%= link_to 'Volver al Curso', course_path(@course), class: 'btn btn-secondary mb-3' %>