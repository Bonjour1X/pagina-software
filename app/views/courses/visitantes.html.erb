<div class="container my-5">
  <%= link_to 'Volver al Inicio', root_path, class: 'btn btn-primary mb-3' %>
  
  <div class="card shadow-lg p-4 mb-4">
    <div class="text-center">
      <h1 class="text-danger">⚠️ Acceso Limitado ⚠️</h1>
      <p class="text-muted">Estás viendo una versión limitada de esta sección. Para acceder al contenido completo, inicia sesión en la aplicación.</p>
    </div>

    <div class="course-info mt-4">
      <h2 class="text-primary"><%= @course.title %></h2>
      <p><strong>Profesor:</strong> <%= @course.user&.name || 'No asignado' %></p>
      <p><strong>Fecha:</strong> <%= @course.scheduled_date %></p>
      <p><strong>Modalidad:</strong> <%= @course.modality %></p>

      <%= link_to course_reviews_path(@course), class: 'btn btn-info mt-3' do %>
        Ver Reseñas (<%= @course.reviews.count %>)
      <% end %>
    </div>
  </div>

  <div class="mt-5">
    <h2 class="text-secondary">📚 Evaluaciones disponibles</h2>
    <p class="text-muted">Solo disponibles para miembros del curso.</p>

    <h2 class="text-secondary mt-4">📂 Documentos públicos</h2>
    <p class="text-muted">Solo disponibles para miembros del curso.</p>

    <h2 class="text-secondary mt-4">💬 Chat</h2>
    <p class="text-muted">Solo disponibles para miembros del curso.</p>
  </div>
</div>
